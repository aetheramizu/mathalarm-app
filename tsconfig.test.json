{
  "//": "Host-testable pure TypeScript: everything under `domain/`, plus the wake-screen reducer. None of it imports React, a native module or SQLite at runtime — the reducer's only cross-layer imports are types, which compile away — so it all runs under `node --test` with no device, no Metro and no test framework dependency.",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022"],
    "module": "commonjs",
    "types": ["node"],
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "paths": { "@/*": ["./src/*"] },
    "rootDir": "src",
    "outDir": ".test-build",
    "noEmitOnError": true
  },
  "include": ["src/domain/**/*.ts", "src/features/wake/machine.ts", "src/features/wake/machine.test.ts"]
}
