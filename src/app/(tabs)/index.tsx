import { Placeholder } from '@/ui/placeholder';
import { Screen } from '@/ui/screen';

export default function AlarmsScreen() {
  return (
    <Screen>
      <Placeholder title="Alarms" arrivesIn="ARRIVES IN P4" />
    </Screen>
  );
}
